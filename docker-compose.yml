version: '3.1'
services:
  burgundy:
    ports:
      - "18889:1313"
    image: eosdaq/burgundy
    networks:
      - eos_net
    depends_on:
      - db
    environment:
      - EOSDAQMANAGE=${EOSDAQMANAGE}
      - EOS_HOST=http://127.0.0.1
      - EOS_PORT=18888
      - DB_HOST=eos_db
      - DB_USER=eosdaquser
      - DB_PASS=eosdaqvotmdnjem
      - ENV=devel
  db:
    ports:
      - "3306:3306"
    image: mariadb
    networks:
      - eos_net
    environment:
        MYSQL_ROOT_PASSWORD: eosdaqvotmdnjem
        MYSQL_DATABASE: eosdaq
        MYSQL_USER: eosdaquser
        MYSQL_PASSWORD: eosdaqvotmdnjem
networks:
  eos_net:
      external: true

